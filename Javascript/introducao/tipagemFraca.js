/**
 * O javascript possui uma tipagem fraca, isto permite flexibilidade, mas também dificulta na hora de analisar os bugs do código.
 * Defina bem o nome da variável, o nome não pode causar ambiguidade. Não use siglas.
 */

 let qualquer = 'legal';
 console.log(typeof qualquer);

 qualquer = 3.5;
 console.log(typeof qualquer); //typeof mostra o tipo da variável

 qualquer = true;
 console.log(typeof qualquer);