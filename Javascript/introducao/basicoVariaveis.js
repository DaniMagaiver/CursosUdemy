/**
 * Os nomes devem refletir o que de fato está fazendo.
 * "let" é a forma mais moderna de criar variáveis.
 * Quando estamos trabalhando com var podemos declarar a variável duas vezes.
 * Quantas mais constantes existirem no código, melhor para mantê-lo. Evite reatribuição de valores.
 */

 var a = 3, b = 4;

 var a = 30;
 //let b = 40; //exibe um erro de tentativa de redeclarar a variável.
 console.log(a);
 console.log(b);
